#include <stdio.h>
/**
 * main - Entry point
 * comment
 * Return: Always 0 (Success)
 */
int main(void)
{
	char strin[50];

	strcpy(strin, "\"Programming is like building a multilingual");
	puts(strin);
	return (0);
}
