#include <stdio.h>
/**
 * main - Entry point
 * comment
 * Return: Always 0 (Success)
 */
int main(void)
{
	char strin[60] = "\"Programming is like building a multilingual puzzle";

	puts(strin);
	return (0);
}
